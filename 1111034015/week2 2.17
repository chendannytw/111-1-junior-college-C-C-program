#include <stdio.h>

int main(void)
{
	int v, a, e, s , v_answer , s_answer;
	    printf("enter v:");
		scanf_s("%d", &v);

		printf("enter a:");
		scanf_s("%d", &a);

		printf("enter e:");
		scanf_s("%d", &e);
		v_answer = v+a*e;
		printf("v is %d\n", v_answer);

		s = v*e+a/2*e*e;
		printf("s is %d\n", s);

		
}
