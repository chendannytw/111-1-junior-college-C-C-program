#include<stdio.h>
#define SIZE 4
#define stu 10
void swap(int* e1Ptr, int* e2Ptr);
void sort (char**name ,int a [][SIZE],int(*compare)(int a, int b));
int asc (int a, int b);
int des (int a, int b);
int main (void)
{int key,re=0,i,j;
int a[10][SIZE]{{2,98,72,0},
{2,98,42,0},
{2,18,72,0},
{2,95,72,0},
{2,68,72,0},
{2,98,72,0},
{2,98,75,0},
{2,98,32,0},
{2,98,12,0},
{2,98,42,0},
} 
cahr*name[13]={"ace",
"tw",
"th",
"fo",
"fi",
"si",
"sve",
"ri",
"ni",
"te"
};
put( "nane id items in oringinal order");
for (j=o :j<10;j++)
{for (i=1 ;j<SZIE-1;++i){
	a[j][3]+=a[j][i];
}
}
for (j=o :j<10;j++)
{
printf("%8s", name[j]);
for (i=0 ;j<SZIE;++i){
	printf("%4d",a[j][i]);
	printf("\n";)
}
}
printf("Enter 1 to sort in asc order,\n"
"Enter 1 to sort in asc order:");
int order;
scanf ("%d",&order);
if(order==1){
	sort(name,a,stu,des);
	put("\nData items in des order");
	
} 
put("\nData items in asc order");
for (j=o :j<10;j++)
{print("%8s",name[j]);
for (i=0 ;j<SZIE;++i){
	printf("%4d",a[j][i]);
}
	printf("\n");
	}
	};
	void (charr**name ,int a [][SIZE],int size,int(*compare)(int a, int b))
{
		char *phr;
	int hold,pass,i;
	for(i=0;i<size;++pass)
	{
	for(i=0;i<size-pass;++i)
	{
	if ((*compare)(a[i][3],a[i+1][3])){
		ptr=mane[i];
		mane[i];=mane[i+1];
		mane[i+1]=ptr;
		swap(&a[i][0],&a[i+1][0])
		swap(&a[i][1],&a[i+1][0])
		swap(&a[i][2],&a[i+1][0])
		swap(&a[i][3],&a[i+1][0])
	}
	}
	}
}
void swap (int *e1Ptr, int *e2ptr)
{int hold;
hold=*e1ptr;
*e1ptr=*e2prt;
*e2ptr=hold;

}
int asc(int a,int b)
{
	return a>b;
}
int des(int a,int b)
{
	return a<b;
}
