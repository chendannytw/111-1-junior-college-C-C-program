#include <iostream>

int fibonacci(int n);
int main()
{
	int n;
	printf("請輸入一個整數:\n");
	scanf_s("%d", &n);
	printf("%d \n", fibonacci(n));
}
int fibonacci(int n)
{
	if (n <= 1)
		return n;
	else
		return fibonacci(n - 1) + fibonacci(n - 2);
}
